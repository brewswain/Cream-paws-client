interface DogFood {
   // probably add enum instead of string for this interface -- avoid typos
   bag_size: string;
   brand: string;
   flavour: string;
   is_wholesale: boolean;
   retail_price: number;
   wholesale_price: number;
   wholesale_price_with_vat: number;
}
